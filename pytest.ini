[pytest]
# Keep all cache/config inside the working tree.
cache_dir = .pytest_cache
testpaths = tests
python_files = test_*.py
markers =
    acceptance: end-to-end-style acceptance tests mapping spec scenarios
